<div class="ui pink inverted vertical footer segment">
  <div class="ui center aligned container">
    <%#= image_tag("logo.jpg", :class => "ui centered tiny image") %>
    <div class="ui list">
      <div class="item">&copy; <%= t :copyright_owner %> 保留所有权利</div>
      <% if user_signed_in? %>
        <div class="item">欢迎您， @<%= current_user.profile.nickname %></div>
      <% end %>
    </div>
  </div>
</div>
